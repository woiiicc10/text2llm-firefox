# v1.0.0 发布说明

## 新增

- 支持在网页选中文字后通过右键菜单发送到大模型网页端。
- 支持模型：豆包、DeepSeek。
- 支持一键发送到默认模型。
- 支持在子菜单中选择模型发送，并自动更新默认模型。

## 体验优化

- 菜单结构调整为：
  - 直接发送到默认模型
  - 选择模型发送...
- 目标页面加载后自动尝试填入选中文本。

## 权限

- contextMenus
- tabs
- storage
- 目标模型站点访问权限

## 兼容性

- Firefox（manifest v2，gecko strict_min_version: 91.0）
